<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Spotify.Views.PlaybackControlPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Spotify.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Pink">

    <StackPanel Grid.Row="2" Orientation="Vertical" Background="#1E1E1E" Padding="10,0">
        <!-- Progress Bar Section -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="CurrentTimeText" Text="0:12" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
            <Slider x:Name="PlaybackSlider" Minimum="0" Maximum="100" Value="20" Margin="10,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Width="400" Background="White"/>
            <TextBlock x:Name="TotalDurationText" Text="3:56" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Playback Controls -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <!-- Shuffle Button -->
            <Button x:Name="ShuffleButton" Width="40" Height="40" Margin="10" Content="🔀" Foreground="White" Background="Transparent"/>
            <!-- Previous Button -->
            <Button x:Name="PreviousButton" Width="40" Height="40" Margin="10" Content="⏮" Foreground="White" Background="Transparent"/>
            <!-- Play/Pause Button -->
            <Button x:Name="PlayPauseButton" Width="60" Height="60" Margin="10" Content="⏯" Foreground="White" Background="Transparent"/>
            <!-- Next Button -->
            <Button x:Name="NextButton" Width="40" Height="40" Margin="10" Content="⏭" Foreground="White" Background="Transparent"/>
            <!-- Repeat Button -->
            <Button x:Name="RepeatButton" Width="40" Height="40" Margin="10" Content="🔁" Foreground="White" Background="Transparent"/>
        </StackPanel>
    </StackPanel>

</Page>
